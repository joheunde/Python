# open(파일 경로, 모드, encoding="UTF-8")
# 모드 => 'r':일기, 'w':쓰기, 'a':추가
#        'w' 또는 'a' 모드로 열린 파일은 닫을 때까지
#        다른 프로그램이 사용할 수 없음 (트랜잭션 : Transaction)

# readline() : 한 줄 읽기
# readlines() : 모든 줄을 읽어서 리스트에 담기
#    (+) 문자열의 strip() : 문자열 끝에 있는 엔터값(개행문자)를 없애줌

# write(문자열) : 문자열 쓰기
# writelines(문자열 리스트) : 리스트에 있는 문자열 쓰기(문자열 간에는 공백 0)

